<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beneficiary Details</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: #121212;
            color: #f5f5f5;
            padding: 2rem;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            width: 100%;
            max-width: 600px;
            background: #1e1e1e;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.5);
            color: #dcdcdc;
        }

        h1 {
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 1.8rem;
            color: #f5f5f5;
        }

        p {
            margin: 0.5rem 0;
            font-size: 1rem;
            line-height: 1.5;
        }

        p strong {
            color: #4caf50;
        }

        a {
            color: #4caf50;
            text-decoration: none;
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }

        button {
            display: inline-block;
            background: #4caf50;
            color: #121212;
            border: none;
            padding: 0.75rem 2rem;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            margin-top: 1rem;
            transition: background-color 0.3s ease, transform 0.2s ease;
        }

        button:hover {
            background: #43a047;
            transform: scale(1.05);
        }

        button:active {
            background: #388e3c;
            transform: scale(0.98);
        }

        button a {
            color: #121212;
            text-decoration: none;
        }

        form {
            margin-top: 1.5rem;
        }

        form label {
            display: block;
            margin-bottom: 0.5rem;
            font-size: 1rem;
            color: #dcdcdc;
        }

        form input {
            width: 100%;
            padding: 0.5rem;
            font-size: 1rem;
            border: 1px solid #333;
            border-radius: 8px;
            margin-bottom: 1rem;
            background: #222;
            color: #dcdcdc;
            outline: none;
        }

        form input:focus {
            border-color: #4caf50;
            box-shadow: 0 0 5px #4caf50;
        }

        form button {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Beneficiary Details</h1>
        <p><strong>Name:</strong> <%= beneficiary.name %></p>
        <p><strong>Age:</strong> <%= beneficiary.age %></p>
        <p><strong>Email:</strong> <%= beneficiary.email %></p>
        <p><strong>Phone Number:</strong> <%= beneficiary.mobile_number %></p>
        <p><strong>Address:</strong> <%= beneficiary.address %></p>
        <p><strong>Problem:</strong> <%= beneficiary.problem %></p>
        <p><strong>Description:</strong> <%= beneficiary.description %></p>
        <p><strong>Date:</strong> <%= beneficiary.created_at %></p>
        <p><strong>Document:</strong> <a href="/download-file/<%= beneficiary.id %>"><%= beneficiary.document_name %></a></p>

        <button><a href="http://localhost:3002/private">Back</a></button>

        <form method="POST" action="http://localhost:3002/view/<%= beneficiary.id %>">
            <label for="amount"><strong>Amount:</strong></label>
            <input required type="number" id="amount" name="amount" placeholder="Enter amount to donate">
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
